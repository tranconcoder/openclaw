services:
  openclaw:
    image: alpine/openclaw:main
    container_name: openclaw_gateway
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./configs/openclaw.json:/home/node/.openclaw/openclaw.json
    env_file:
      - .env
